[package]
name = "des-sim"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
rand_distr = "0.6.0"
rayon = "1.12.0"
log = "0.4.33"

[dev-dependencies]
env_logger = "0.11.10"
chrono = "0.4.45"

[features]
# デフォルトでは有効にしない (無効化)
default = []
# 詳細デバッグを有効にするフラグ
verbose_debug = []